{
  "name": "project",
  "version": "0.1.0",
  "private": true,
  "devDependencies": {
    "@sap/approuter": "14.4.2",
    "@sap/html5-repo-mock": "2.1.5"
  },
  "scripts": {
    "start": "cds-serve",
    "start:app": "cds bind --exec -- npm start --prefix vuejsapp-approuter",    
    "build-vuejsapp": "cd vuejsapp && if [ ! -d ${workspaceFolder}/vuejsapp/vuejsapp/node_modules/@vue/cli-service ] && [ ! -d ${workspaceFolder}/vuejsapp/node_modules/@vue/cli-service ]; then npm i; fi && npm run build"
  }
}
